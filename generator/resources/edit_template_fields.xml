<?xml version="1.0" encoding="utf-8"?>
<!-- This file is a code generation template for Chisimba edit template data items -->
<chisimba_edit_template_fields>

<item name="id" type="datafield">
<description>Generates the hidden text input for the id item</description>
<code>
//Set the value to the primary key: id
if (isset($id)) {
    //Create an element for the hidden text input for the id PK item
    $objElement = new textinput("id");
    $objElement->setValue($id);
    //Set the item type to hidden for the primary key
    $objElement->fldType="hidden";
    //Add the hidden PK item to the form
    $objForm->addToForm($objElement->show());
}
</code>
</item>

<item name="textinput" type="datafield">
<description>Generates a text input for the relatively short text items</description>
<code>
//Set the value of the element to {FIELDNAME}
if (isset(${FIELDNAME})) {
    //Create a text input element for ${FIELDNAME}
    $objElement = new textinput("{FIELDNAME}");
    $objElement->setValue(${FIELDNAME});
    //Add the ${FIELDNAME} element to the form
    $objForm->addToForm($this->objLanguage->languageText("mod_{MODULECODE}_{FIELDNAME}", 
      {MODULECODE}) . "<br />" . $objElement->show() . "<br /><br />");
}
</code>
</item>

<item name="textarea" type="datafield">
<description>Generates a text area for the longer text items</description>
<code>
//Set the value of the element to {FIELDNAME}
if (isset(${FIELDNAME})) {
    //Create a text area element for {FIELDNAME}
    $objElement = new textarea("{FIELDNAME}");
    $objElement->setContent(${FIELDNAME});
    //Add the ${FIELDNAME} element to the form
    $objForm->addToForm($this->objLanguage->languageText("mod_{MODULECODE}_{FIELDNAME}", 
      {MODULECODE}) . "<br />" . $objElement->show() . "<br /><br />");
}
</code>
</item>

<item name="dropdown" type="datafield">
<description>Generates a dropdown for the values obtained from a supplied array</description>
<code>Todo</code>
</item>

<item name="radio" type="datafield">
<description>Generates a radio input for the values obtained from a supplied array</description>
<code>
//Pass data into the array for the radio items
{ARRAY_FOR_RADIO}
//Create a radio element for {FIELDNAME}
$objElement = new radio ("{FIELDNAME}");
foreach ($ar as $item) {
    $objElement->addOption($item, $item);
}
return $objElement->show();</code>
</item>

<item name="wyswyg_simple" type="datafield">
<description>Generates a simple wysyg input for the value of a text item</description>
<code>--------------------Todo</code>
</item>

<item name="wyswyg_full" type="datafield">
<description>Generates a full wysyg input for the value of a text item</description>
<code>--------------------Todo</code>
</item>

</chisimba_edit_template_fields>